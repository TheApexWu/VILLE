# docs-notes

Loop-local notes between iterations. Terse by design — the manifesto + PRD are the
source of truth; this folder is for environment gotchas and handoffs the next
iteration needs to know without rediscovering.
